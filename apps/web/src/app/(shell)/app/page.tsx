import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'App' };

export default function AppPage() {
  return <p className="text-muted-foreground">Reserved for future sprint.</p>;
}
